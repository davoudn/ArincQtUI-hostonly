#include "Handler.h"
#include "DataSourceBase.h"

void Handler::setDataSource(DataSourceBase* dataSource)
{
    if (dataSource) {
        dataSource = dataSource;
        //dataSource->setDataHandler(this);
    }
}
}
